# Legacy Deployment Artifacts

> **Note (v2.0 Release)**: This directory contains legacy AWS SAM packaging templates and configuration from v1 prototype deployments.
> In v2, the primary architecture is a modular monolith containerized via the root `backend/Dockerfile` as specified in [ARCHITECTURE.md](../../docs/v2-release/ARCHITECTURE.md).
> Do not manually copy code into this folder.